sudo: false
language: java
jdk:
    - openjdk8
cache:
    directories:
        - $HOME/.m2'
        - $HOME/.sonar/cache

addons:
    sonarcloud:
        organization: "mkarpisek-github"
        
install: true
script: mvn clean package sonar:sonar
